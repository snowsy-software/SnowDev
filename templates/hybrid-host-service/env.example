# Copy to .env and adjust. SnowDev merges, lowest priority first:
#   config defaults < .env < .env.<profile> < .env.local < .env.<profile>.local < process env
DATABASE_URL=postgres://app:app@localhost:5432/app
SERVER_PORT=8080
